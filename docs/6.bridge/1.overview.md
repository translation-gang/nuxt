---
title: "Обзор"
description: "Сближение с Nuxt 3 и упрощение миграции на Nuxt 3."
---

::note
Если вы начинаете новый проект на Nuxt 3, пропустите этот раздел и перейдите к [установке Nuxt 3](/docs/4.x/getting-started/introduction).
::

::warning
Nuxt Bridge даёт те же возможности, что и Nuxt 3 ([документация](/docs/4.x/guide/concepts/auto-imports)), но с ограничениями: компосаблы [`useAsyncData`](/docs/4.x/api/composables/use-async-data) и [`useFetch`](/docs/4.x/api/composables/use-fetch) недоступны. Подробности — на этой странице.
::

Bridge — слой прямой совместимости: вы получаете многие возможности Nuxt 3, установив и включив один модуль.

С Nuxt Bridge проект можно подготовить к Nuxt 3 и переходить к нему по шагам.

## Первый шаг

### Обновление Nuxt 2

Остановите dev-сервер (`nuxt dev`), удалите lock-файлы (`package-lock.json`, `yarn.lock`), установите последнюю Nuxt 2:

```diff [package.json]
- "nuxt": "^2.16.3"
+ "nuxt": "^2.17.3"
```

Затем переустановите зависимости:

::code-group{sync="pm"}

```bash [npm]
npm install
```

```bash [yarn]
yarn install
```

```bash [pnpm]
pnpm install
```

```bash [bun]
bun install
```

```bash [deno]
deno install
```

::

::note
После установки убедитесь, что и dev, и production-сборка работают, прежде чем продолжать.
::

### Установка Nuxt Bridge

Установите `@nuxt/bridge` и `nuxi` как dev-зависимости:

::code-group{sync="pm"}

```bash [npm]
npm install -D @nuxt/bridge nuxi
```

```bash [yarn]
yarn add --dev @nuxt/bridge nuxi
```

```bash [pnpm]
pnpm add -D @nuxt/bridge nuxi
```

```bash [bun]
bun add -D @nuxt/bridge nuxi
```

```bash [deno]
deno add -D npm:@nuxt/bridge npm:nuxi
```

::

### Обновление `nuxt.config`

Не используйте в конфиге CommonJS: `module.exports`, `require`, `require.resolve` — это скоро будет считаться устаревшим.

Используйте статический `import`, динамический `import()` и `export default`. Рекомендуется перейти на TypeScript и переименовать файл в [`nuxt.config.ts`](/docs/4.x/directory-structure/nuxt-config).

```ts [nuxt.config.ts]
import { defineNuxtConfig } from '@nuxt/bridge'

export default defineNuxtConfig({
  bridge: false,
})
```

### Обновление скриптов

Команду `nuxt` замените на `nuxt2`:

```diff
{
  "scripts": {
-   "dev": "nuxt",
+   "dev": "nuxt2",
-   "build": "nuxt build",
+   "build": "nuxt2 build",
-   "start": "nuxt start",
+   "start": "nuxt2 start"
  }
}
```

Запустите `nuxt2` — приложение должно работать как раньше.

(Если `bridge` установлен в `false`, приложение работает без изменений.)

## Шаги обновления

С Nuxt Bridge миграция на Nuxt 3 может быть пошаговой. Выполнять все шаги сразу не обязательно.

- [TypeScript](/docs/4.x/bridge/typescript)

- [Миграция старого Composition API](/docs/4.x/bridge/bridge-composition-api)

- [Плагины и middleware](/docs/4.x/bridge/plugins-and-middleware)

- [Миграция на новый Composition API](/docs/4.x/bridge/nuxt3-compatible-api)

- [Мета-теги](/docs/4.x/bridge/meta)

- [Runtime Config](/docs/4.x/bridge/runtime-config)

- [Nitro](/docs/4.x/bridge/nitro)

- [Vite](/docs/4.x/bridge/vite)

## Переход с CommonJS на ESM

Nuxt 3 нативно поддерживает TypeScript и ES-модули. Подробнее — [нативные ES-модули](/docs/4.x/guide/concepts/esm).
