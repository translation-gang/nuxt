---
title: "Отладка"
description: "Отладка приложения Nuxt в браузере и в IDE."
---

## Source maps

Для серверной сборки source maps включены по умолчанию, для клиента — в dev; при необходимости задайте явно в конфиге.

```ts
export default defineNuxtConfig({
  // or sourcemap: true
  sourcemap: {
    server: true,
    client: true,
  },
})
```

## Node Inspector

[Node inspector](https://nodejs.org/en/learn/getting-started/debugging) для отладки серверной части Nuxt.

```bash
nuxt dev --inspect
```

Запускает `dev` с активным отладчиком. В Chrome DevTools должен появиться значок Node.js — к нему можно подключиться.

::important
Процессы Node.js и Chrome должны быть на одной платформе. Внутри Docker обычно не работает.
::

## Отладка в IDE

Nuxt можно отлаживать в IDE во время разработки.

### Пример для VS Code

Укажите путь к браузеру при необходимости. Подробнее — [документация VS Code по launch](https://code.visualstudio.com/docs/debugtest/debugging#_launch-configurations).

```json5
{
  // Use IntelliSense to learn about possible attributes.
  // Hover to view descriptions of existing attributes.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "client: chrome",
      "url": "http://localhost:3000",
      // this should point to your Nuxt `srcDir`, which is `app` by default
      "webRoot": "${workspaceFolder}/app"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "server: nuxt",
      "outputCapture": "std",
      "program": "${workspaceFolder}/node_modules/nuxt/bin/nuxt.mjs",
      "args": [
        "dev"
      ],
    }
  ],
  "compounds": [
    {
      "name": "fullstack: nuxt",
      "configurations": [
        "server: nuxt",
        "client: chrome"
      ]
    }
  ]
}
```

Для отладки через расширение браузера добавьте в конфигурацию _chrome_:

```json5
"userDataDir": false,
```

### Пример для JetBrains (IDEA, WebStorm, PhpStorm)

1. В корне проекта создайте файл `nuxt.run.xml`.

2. Вставьте конфигурацию:

```html
<component name="ProjectRunConfigurationManager">
  <configuration default="false" name="client: chrome" type="JavascriptDebugType" uri="http://localhost:3000" useFirstLineBreakpoints="true">
    <method v="2" />
  </configuration>

  <configuration default="false" name="server: nuxt" type="NodeJSConfigurationType" application-parameters="dev" path-to-js-file="$PROJECT_DIR$/node_modules/nuxt/bin/nuxt.mjs" working-dir="$PROJECT_DIR$">
    <method v="2" />
  </configuration>

  <configuration default="false" name="fullstack: nuxt" type="CompoundRunConfigurationType">
    <toRun name="client: chrome" type="JavascriptDebugType" />
    <toRun name="server: nuxt" type="NodeJSConfigurationType" />
    <method v="2" />
  </configuration>
</component>
```

### Другие IDE

Примеры для других IDE приветствуются — [откройте PR](https://github.com/nuxt/nuxt/edit/main/docs/2.guide/3.going-further/9.debugging.md).
