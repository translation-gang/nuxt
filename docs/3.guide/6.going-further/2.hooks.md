---
title: "Хуки жизненного цикла"
description: "Система хуков Nuxt почти на каждом этапе позволяет расширять поведение."
---

::tip
Реализовано на [unjs/hookable](https://github.com/unjs/hookable).
::

## Хуки Nuxt (этап сборки)

Доступны в [модулях Nuxt](/docs/4.x/guide/modules) и контексте сборки.

### В `nuxt.config.ts`

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  hooks: {
    close: () => { },
  },
})
```

### В модулях Nuxt

```js
import { defineNuxtModule } from '@nuxt/kit'

export default defineNuxtModule({
  setup (options, nuxt) {
    nuxt.hook('close', async () => { })
  },
})
```

::read-more{to="/docs/4.x/api/advanced/hooks#nuxt-hooks-build-time"}
Полный список хуков Nuxt.
::

## Хуки приложения (runtime)

В основном в [плагинах Nuxt](/docs/4.x/directory-structure/app/plugins) для жизненного цикла рендеринга; также можно использовать в композаблах Vue.

```ts [app/plugins/test.ts]
export default defineNuxtPlugin((nuxtApp) => {
  nuxtApp.hook('page:start', () => {
    /* your code goes here */
  })
})
```

::read-more{to="/docs/4.x/api/advanced/hooks#app-hooks-runtime"}
Полный список хуков приложения.
::

## Серверные хуки (runtime)

Для [плагинов сервера](/docs/4.x/directory-structure/server#server-plugins) и поведения Nitro в runtime.

```ts [~~/server/plugins/test.ts]
export default defineNitroPlugin((nitroApp) => {
  nitroApp.hooks.hook('render:html', (html, { event }) => {
    console.log('render:html', html)
    html.bodyAppend.push('<hr>Appended by custom plugin')
  })

  nitroApp.hooks.hook('render:response', (response, { event }) => {
    console.log('render:response', response)
  })
})
```

::read-more{to="/docs/4.x/api/advanced/hooks#nitro-app-hooks-runtime-server-side"}
Хуки жизненного цикла Nitro.
::

## Свои хуки

Можно расширить типы хуков Nuxt под свои имена.

```ts
import type { HookResult } from '@nuxt/schema'

declare module '#app' {
  interface RuntimeNuxtHooks {
    'your-nuxt-runtime-hook': () => HookResult
  }
  interface NuxtHooks {
    'your-nuxt-hook': () => HookResult
  }
}

declare module 'nitropack/types' {
  interface NitroRuntimeHooks {
    'your-nitro-hook': () => void
  }
}
```
