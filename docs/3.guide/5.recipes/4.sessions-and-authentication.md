---
title: 'Сессии и аутентификация'
description: "Аутентификация — частая задача. Рецепт: базовая регистрация входа и сессии в full-stack приложении Nuxt."
---

## Введение

Настроим аутентификацию на [Nuxt Auth Utils](https://github.com/Atinux/nuxt-auth-utils): утилиты для сессии на клиенте и сервере.

Сессия хранится в подписанных cookie, отдельная БД для сессий не обязательна.

## Установка nuxt-auth-utils

```bash [Terminal]
npx nuxt module add auth-utils
```

::callout
Команда поставит `nuxt-auth-utils` и добавит модуль в `nuxt.config.ts`.
::

## Ключ шифрования cookie

Сессионные cookie шифруются секретом из `NUXT_SESSION_PASSWORD`.

::note
В dev при отсутствии переменной она может быть добавлена в `.env` автоматически.
::

```ini [.env]
NUXT_SESSION_PASSWORD=a-random-password-with-at-least-32-characters
```

::important
Перед деплоем в production задайте эту переменную в окружении.
::

## API входа

Простой маршрут входа на статических данных.

POST `/api/login` с email и password в теле:

```ts [server/api/login.post.ts]
import { z } from 'zod'

const bodySchema = z.object({
  email: z.email(),
  password: z.string().min(8),
})

export default defineEventHandler(async (event) => {
  const { email, password } = await readValidatedBody(event, bodySchema.parse)

  if (email === 'admin@admin.com' && password === 'iamtheadmin') {
    // set the user session in the cookie
    // this server util is auto-imported by the auth-utils module
    await setUserSession(event, {
      user: {
        name: 'John Doe',
      },
    })
    return {}
  }
  throw createError({
    status: 401,
    message: 'Bad credentials',
  })
})
```

::callout
Установите зависимость `zod` (`npm i zod`).
::

::tip{to="https://github.com/atinux/nuxt-auth-utils#server-utils"}
Серверный хелпер `setUserSession` в документации nuxt-auth-utils.
::

## Страница входа

Композабл для проверки авторизации:

```vue
<script setup>
const { loggedIn, session, user, clear, fetch } = useUserSession()
</script>
```

Форма, отправляющая данные на `/api/login`:

```vue [app/pages/login.vue]
<script setup lang="ts">
const { loggedIn, user, fetch: refreshSession } = useUserSession()
const credentials = reactive({
  email: '',
  password: '',
})
async function login () {
  try {
    await $fetch('/api/login', {
      method: 'POST',
      body: credentials,
    })

    // Refresh the session on client-side and redirect to the home page
    await refreshSession()
    await navigateTo('/')
  } catch {
    alert('Bad credentials')
  }
}
</script>

<template>
  <form @submit.prevent="login">
    <input
      v-model="credentials.email"
      type="email"
      placeholder="Email"
    >
    <input
      v-model="credentials.password"
      type="password"
      placeholder="Password"
    >
    <button type="submit">
      Login
    </button>
  </form>
</template>
```

## Защита API

Клиентский middleware удобен пользователю, но без проверки на сервере данные остаются уязвимыми. Для чувствительных маршрутов возвращайте 401 неавторизованным.

Модуль даёт `requireUserSession`:

```ts [server/api/user/stats.get.ts]
export default defineEventHandler(async (event) => {
  // make sure the user is logged in
  // This will throw a 401 error if the request doesn't come from a valid user session
  const { user } = await requireUserSession(event)

  // TODO: Fetch some stats based on the user

  return {}
})
```

## Защита маршрутов приложения

Чтобы неавторизованный пользователь не попадал на `/users` с «ломаным» UI, добавьте [middleware на клиенте](https://nuxt.com/docs/4.x/directory-structure/app/middleware) с редиректом на логин.

Через `useUserSession` проверяем `loggedIn`. Файл в `middleware` сам по себе ни на что не вешается — укажите его в метаданных страницы.

```typescript [app/middleware/authenticated.ts]
export default defineNuxtRouteMiddleware(() => {
  const { loggedIn } = useUserSession()

  // redirect the user to the login screen if they're not authenticated
  if (!loggedIn.value) {
    return navigateTo('/login')
  }
})
```

## Главная страница

Подключаем middleware к защищаемой странице через [`definePageMeta`](/docs/4.x/api/utils/define-page-meta).

```vue [app/pages/index.vue]
<script setup lang="ts">
definePageMeta({
  middleware: ['authenticated'],
})

const { user, clear: clearSession } = useUserSession()

async function logout () {
  await clearSession()
  await navigateTo('/login')
}
</script>

<template>
  <div>
    <h1>Welcome {{ user.name }}</h1>
    <button @click="logout">
      Logout
    </button>
  </div>
</template>
```

Кнопка выхода очищает сессию и ведёт на `/login`.

## Итог

Настроены базовый вход, сессия и защита API и клиентских маршрутов.

Дальше можно:
- подключить [20+ OAuth-провайдеров](https://github.com/atinux/nuxt-auth-utils?tab=readme-ov-file#supported-oauth-providers)
- хранить пользователей в БД: [Nitro SQL](https://nitro.build/guide/database) или [NuxtHub SQL](https://hub.nuxt.com/docs/database)
- регистрация по email/паролю с [хешированием пароля](https://github.com/atinux/nuxt-auth-utils?tab=readme-ov-file#password-hashing)
- [WebAuthn / Passkeys](https://github.com/atinux/nuxt-auth-utils?tab=readme-ov-file#webauthn-passkey)

Полный пример с OAuth и БД — репозиторий [atidone](https://github.com/atinux/atidone).
