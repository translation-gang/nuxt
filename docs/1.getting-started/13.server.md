---
title: 'Сервер'
description: Full-stack приложения на серверном фреймворке Nuxt: запросы к БД или другим серверам, API, статический контент (sitemap, RSS) из одной кодовой базы.
navigation.icon: i-lucide-pc-case
---

:read-more{to="/docs/4.x/directory-structure/server"}

## На базе Nitro

![Серверный движок](/assets/docs/getting-started/server.svg)

Сервер Nuxt — это [Nitro](https://github.com/nitrojs/nitro). Изначально он создавался для Nuxt, сейчас входит в [UnJS](https://unjs.io) и может использоваться другими фреймворками или отдельно.

Nitro даёт Nuxt ряд возможностей:

- Полный контроль серверной части приложения
- Универсальное развёртывание у любого провайдера (часто без конфигурации)
- Гибридный рендеринг

Внутри Nitro использует [h3](https://github.com/h3js/h3) — минималистичный HTTP-фреймворк с высокой производительностью и переносимостью.

:video-accordion{title="Видео Alexander Lichter: роли Nuxt и Nitro в приложении" videoId="DkvgJa-X31k"}

## API и middleware

Серверную часть Nuxt (API и middleware) можно описывать единообразно:

```ts twoslash [server/api/test.ts]
export default defineEventHandler(async (event) => {
  // ... Ваша логика
})
```

Возвращать можно `text`, `json`, `html` или даже `stream`.

Как и в остальном Nuxt, поддерживаются **hot module replacement** и **автоимпорт**.

:read-more{to="/docs/4.x/directory-structure/server"}

## Универсальное развёртывание

Nitro позволяет развернуть Nuxt где угодно — от bare metal до edge, с запуском за миллисекунды.

:read-more{to="/blog/nuxt-on-the-edge"}

Доступно более 15 пресетов под разные облака и серверы, в том числе:

- [Cloudflare Workers](https://workers.cloudflare.com)
- [Netlify Functions](https://www.netlify.com/platform/core/functions/)
- [Vercel Cloud](https://vercel.com/home)

И под другие рантаймы:

::card-group
  :card{icon="i-logos-deno" title="Deno" to="https://deno.com" target="_blank"}
  :card{icon="i-logos-bun" title="Bun" to="https://bun.com" target="_blank"}
::

:read-more{to="/docs/4.x/getting-started/deployment"}

## Гибридный рендеринг

В Nitro есть механизм `routeRules`: для каждого маршрута можно задать правила рендеринга и не только.

```ts twoslash [nuxt.config.ts]
export default defineNuxtConfig({
  routeRules: {
    // Генерируется при сборке для SEO
    '/': { prerender: true },
    // Кэш на 1 час
    '/api/*': { cache: { maxAge: 60 * 60 } },
    // Редирект вместо 404
    '/old-page': {
      redirect: { to: '/new-page', statusCode: 302 },
    },
    // ...
  },
})
```

::read-more{to="/docs/4.x/guide/concepts/rendering#hybrid-rendering"}
Подробнее о правилах маршрутов и режимах рендеринга.
::

Часть правил (`ssr`, `appMiddleware`, `noScripts`) относится к Nuxt и меняет поведение при рендере страниц в HTML.

Некоторые правила (`appMiddleware`, `redirect`, `prerender`) влияют и на клиентское поведение.

Nitro используется и для серверного рендеринга, и для пререндеринга.

:read-more{to="/docs/4.x/guide/concepts/rendering"}
