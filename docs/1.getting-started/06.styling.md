---
title: 'Стили'
description: 'Как стилизовать приложение Nuxt.'
navigation.icon: i-lucide-palette
---

Nuxt даёт большую свободу в стилизации: свои стили, локальные и внешние таблицы, препроцессоры, CSS-фреймворки, UI-библиотеки и модули Nuxt.

## Локальные стили

Для локальных стилей естественное место — [директория `app/assets/`](/docs/4.x/directory-structure/app/assets).

### Импорт в компонентах

Стили можно импортировать в страницах, макетах и компонентах — через JavaScript-импорт или CSS-[`@import`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@import).

```vue [app/pages/index.vue]
<script>
// Статический импорт для совместимости с SSR
import '~/assets/css/first.css'

// Внимание: динамический импорт не совместим с SSR
import('~/assets/css/first.css')
</script>

<style>
@import url("~/assets/css/second.css");
</style>
```

::tip
Стили будут встроены в HTML, отдаваемый Nuxt.
::

### Свойство css в конфиге

Стили можно подключить через свойство `css` в конфигурации Nuxt. Удобное место для файлов — [директория `app/assets/`](/docs/4.x/directory-structure/app/assets); укажите путь, и Nuxt подключит его ко всем страницам.

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  css: ['~/assets/css/main.css'],
})
```

::tip
Стили будут встроены в HTML, подключены глобально и будут на всех страницах.
::

### Работа со шрифтами

Разместите файлы шрифтов в `public/`, например в `public/fonts`. В стилях подключайте их через `url()`.

```css [assets/css/main.css]
@font-face {
  font-family: 'FarAwayGalaxy';
  src: url('/fonts/FarAwayGalaxy.woff') format('woff');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}
```

Далее используйте имя шрифта в стилях, страницах или компонентах:

```vue
<style>
h1 {
  font-family: 'FarAwayGalaxy', sans-serif;
}
</style>
```

### Стили из npm

Можно подключать стили из npm-пакетов. Пример — библиотека `animate.css`:

::code-group{sync="pm"}

```bash [npm]
npm install animate.css
```

```bash [yarn]
yarn add animate.css
```

```bash [pnpm]
pnpm install animate.css
```

```bash [bun]
bun install animate.css
```

```bash [deno]
deno install npm:animate.css
```

::

Подключение в страницах, макетах и компонентах:

```vue [app/app.vue]
<script>
import 'animate.css'
</script>

<style>
@import url("animate.css");
</style>
```

Пакет можно указать строкой в свойстве `css` в конфиге Nuxt:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  css: ['animate.css'],
})
```

## Внешние стили

Внешние таблицы стилей подключаются через элемент `link` в `head` — например, через свойство [`app.head`](/docs/4.x/api/nuxt-config#head) в конфиге Nuxt. Так же можно подключать и локальные стили.

```ts twoslash [nuxt.config.ts]
export default defineNuxtConfig({
  app: {
    head: {
      link: [{ rel: 'stylesheet', href: 'https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css' }],
    },
  },
})
```

### Динамическое добавление стилей

Композабл `useHead` позволяет задавать теги в `head` из кода.

:read-more{to="/docs/4.x/api/composables/use-head"}

```ts twoslash
useHead({
  link: [{ rel: 'stylesheet', href: 'https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css' }],
})
```

Под капотом Nuxt использует `unhead` — см. [документацию](https://unhead.unjs.io).

### Изменение head через Nitro-плагин

Для более тонкого контроля можно перехватить отрендеренный HTML хуком и изменить `head` в коде.

Создайте плагин в `~~/server/plugins/my-plugin.ts`:

```ts [server/plugins/my-plugin.ts]
export default defineNitroPlugin((nitro) => {
  nitro.hooks.hook('render:html', (html) => {
    html.head.push('<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">')
  })
})
```

Внешние стили блокируют рендеринг: браузер должен загрузить и обработать их до отрисовки страницы. Подробнее: [web.dev](https://web.dev/articles/defer-non-critical-css).

## Препроцессоры

Для использования SCSS, Sass, Less или Stylus сначала установите нужный пакет:

::code-group

```bash [Sass & SCSS]
npm install -D sass
```

```bash [Less]
npm install -D less
```

```bash [Stylus]
npm install -D stylus
```

::

Писать стили удобно в директории `app/assets`. Импортируйте их в `app.vue` (или в макетах) синтаксисом выбранного препроцессора:

```vue [app/pages/app.vue]
<style lang="scss">
@use "~/assets/scss/main.scss";
</style>
```

Либо укажите файл в свойстве `css` конфига Nuxt:

```ts twoslash [nuxt.config.ts]
export default defineNuxtConfig({
  css: ['~/assets/scss/main.scss'],
})
```

::tip
В обоих случаях скомпилированные стили будут встроены в HTML, отдаваемый Nuxt.
::

Чтобы внедрить код в обрабатываемые файлы (например, [Sass-партиал](https://sass-lang.com/documentation/at-rules/use/#partials) с переменными цветов), используйте [опции препроцессоров Vite](https://vite.dev/config/shared-options#css-preprocessoroptions).

Создайте партиалы в `app/assets`:

::code-group{sync="preprocessor"}

```scss [assets/_colors.scss]
$primary: #49240F;
$secondary: #E4A79D;
```

```sass [assets/_colors.sass]
$primary: #49240F
$secondary: #E4A79D
```

::

В `nuxt.config`:

::code-group

```ts twoslash [SCSS]
export default defineNuxtConfig({
  vite: {
    css: {
      preprocessorOptions: {
        scss: {
          additionalData: '@use "~/assets/_colors.scss" as *;',
        },
      },
    },
  },
})
```

```ts twoslash [SASS]
export default defineNuxtConfig({
  vite: {
    css: {
      preprocessorOptions: {
        sass: {
          additionalData: '@use "~/assets/_colors.sass" as *\n',
        },
      },
    },
  },
})
```

::

По умолчанию Nuxt использует Vite. При использовании webpack см. документацию [лоадеров](https://webpack.js.org/loaders/sass-loader/) для каждого препроцессора.

### Preprocessor Workers (экспериментально)

В Vite есть [экспериментальная опция](https://vite.dev/config/shared-options#css-preprocessormaxworkers) для ускорения препроцессоров.

Включение в `nuxt.config`:

```ts
export default defineNuxtConfig({
  vite: {
    css: {
      preprocessorMaxWorkers: true, // число CPU минус 1
    },
  },
})
```

::note
Опция экспериментальная; см. документацию Vite и [обсуждение](https://github.com/vitejs/vite/discussions/15835).
::

## Стили в SFC (Single File Components)

В Vue и SFC стили задаются прямо в блоке `<style>` компонента — без обязательного CSS-in-JS. При желании можно использовать сторонние библиотеки вроде [pinceau](https://github.com/Tahul/pinceau).

Подробнее: [документация Vue по стилям в SFC](https://vuejs.org/api/sfc-css-features).

### Привязки class и style

Используйте возможности Vue SFC для привязки атрибутов `class` и `style`:

::code-group

```vue [Ref и Reactive]
<script setup lang="ts">
const isActive = ref(true)
const hasError = ref(false)
const classObject = reactive({
  'active': true,
  'text-danger': false,
})
</script>

<template>
  <div
    class="static"
    :class="{ 'active': isActive, 'text-danger': hasError }"
  />
  <div :class="classObject" />
</template>
```

```vue [Computed]
<script setup lang="ts">
const isActive = ref(true)
const error = ref(null)

const classObject = computed(() => ({
  'active': isActive.value && !error.value,
  'text-danger': error.value && error.value.type === 'fatal',
}))
</script>

<template>
  <div :class="classObject" />
</template>
```

```vue [Array]
<script setup lang="ts">
const isActive = ref(true)
const errorClass = ref('text-danger')
</script>

<template>
  <div :class="[{ active: isActive }, errorClass]" />
</template>
```

```vue [Style]
<script setup lang="ts">
const activeColor = ref('red')
const fontSize = ref(30)
const styleObject = reactive({ color: 'red', fontSize: '13px' })
</script>

<template>
  <div :style="{ color: activeColor, fontSize: fontSize + 'px' }" />
  <div :style="[baseStyles, overridingStyles]" />
  <div :style="styleObject" />
</template>
```

::

Подробнее: [Vue — классы и стили](https://vuejs.org/guide/essentials/class-and-style).

### Динамические стили с `v-bind`

В блоках `<style>` можно использовать переменные и выражения JavaScript через `v-bind`. Значения реактивны: при изменении переменной обновится и стиль.

```vue
<script setup lang="ts">
const color = ref('red')
</script>

<template>
  <div class="text">
    hello
  </div>
</template>

<style>
.text {
  color: v-bind(color);
}
</style>
```

### Scoped-стили

Атрибут `scoped` ограничивает стили текущим компонентом.

```vue
<template>
  <div class="example">
    hi
  </div>
</template>

<style scoped>
.example {
  color: red;
}
</style>
```

### CSS Modules

[CSS Modules](https://github.com/css-modules/css-modules) подключаются атрибутом `module`. Доступ через переменную `$style`:

```vue
<template>
  <p :class="$style.red">
    This should be red
  </p>
</template>

<style module>
.red {
  color: red;
}
</style>
```

### Препроцессоры в SFC

В блоках `<style>` поддерживается синтаксис препроцессоров. Vite из коробки поддерживает .scss, .sass, .less, .styl и .stylus — достаточно установить пакет и указать `lang`:

::code-group

```vue [SCSS]
<style lang="scss">
  /* Write scss here */
</style>
```

```vue [Sass]
<style lang="sass">
  /* Write sass here */
</style>
```

```vue [LESS]
<style lang="less">
  /* Write less here */
</style>
```

```vue [Stylus]
<style lang="stylus">
  /* Write stylus here */
</style>
```

::

См. [Vite CSS](https://vite.dev/guide/features#css) и [@vitejs/plugin-vue](https://github.com/vitejs/vite-plugin-vue/tree/main/packages/plugin-vue). Для webpack — [vue-loader](https://vue-loader.vuejs.org).

## PostCSS

В Nuxt PostCSS встроен. Настройка в `nuxt.config`:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  postcss: {
    plugins: {
      'postcss-nested': {},
      'postcss-custom-media': {},
    },
  },
})
```

Для подсветки синтаксиса в SFC можно указать `lang="postcss"`:

```vue
<style lang="postcss">
  /* Write postcss here */
</style>
```

По умолчанию в Nuxt уже подключены:

- [postcss-import](https://github.com/postcss/postcss-import): улучшает правило `@import`
- [postcss-url](https://github.com/postcss/postcss-url): преобразует `url()`
- [autoprefixer](https://github.com/postcss/autoprefixer): вендорные префиксы
- [cssnano](https://cssnano.github.io/cssnano/): минификация и очистка

## Разные стили для макетов

Если разным частям приложения нужны разные стили, используйте макеты и задавайте стили для каждого макета.

```vue
<template>
  <div class="default-layout">
    <h1>Default Layout</h1>
    <slot />
  </div>
</template>

<style>
.default-layout {
  color: red;
}
</style>
```

:read-more{to="/docs/4.x/directory-structure/app/layouts"}

## Сторонние библиотеки и модули

Nuxt не навязывает подход к стилям: можно использовать [UnoCSS](https://unocss.dev), [Tailwind CSS](https://tailwindcss.com) и другие инструменты.

Сообщество и команда Nuxt разработали множество модулей для удобной интеграции. Ищите их в [разделе модулей](/modules) сайта. Несколько примеров:

- [UnoCSS](/modules/unocss): атомарный CSS по требованию
- [Tailwind CSS](/modules/tailwindcss): utility-first CSS фреймворк
- [Fontaine](https://github.com/nuxt-modules/fontaine): метрики шрифтов и fallback
- [Pinceau](https://github.com/Tahul/pinceau): адаптивная система стилей
- [Nuxt UI](https://ui.nuxt.com): UI-библиотека для современных приложений
- [Panda CSS](https://panda-css.com/docs/installation/nuxt): CSS-in-JS с генерацией атомарного CSS при сборке

Модули дают удобный опыт из коробки, но если у вашего инструмента нет модуля — его всё равно можно подключить вручную (плагин и/или [собственный модуль](/docs/4.x/guide/modules)). Имеет смысл поделиться решением в [сообществе](/modules).

### Подключение веб-шрифтов

Для Google Fonts можно использовать [модуль Nuxt Google Fonts](https://github.com/nuxt-modules/google-fonts).

При использовании [UnoCSS](https://unocss.dev/integrations/nuxt) доступен [пресет web fonts](https://unocss.dev/presets/web-fonts) для подключения шрифтов от популярных провайдеров, включая Google Fonts.

## Продвинутое

### Переходы

В Nuxt доступен тот же элемент `<Transition>`, что и во Vue, плюс экспериментальная [View Transitions API](/docs/4.x/getting-started/transitions#view-transitions-api-experimental).

:read-more{to="/docs/4.x/getting-started/transitions"}

### Оптимизация шрифтов

Рекомендуется [Fontaine](https://github.com/nuxt-modules/fontaine) для снижения [CLS](https://web.dev/articles/cls). Для более сложных сценариев можно написать модуль Nuxt, расширяющий сборку или рантайм.

::tip
Используйте доступные в веб-экосистеме инструменты: нативный CSS, препроцессор, PostCSS, UI-библиотеку или модуль — Nuxt позволяет комбинировать их как удобно.
::

### Оптимизация LCP

Чтобы ускорить загрузку глобального CSS:

- Используйте CDN для приближения файлов к пользователям
- Сжимайте ресурсы (предпочтительно Brotli)
- Используйте HTTP/2 или HTTP/3
- Размещайте ресурсы на том же домене (без отдельного поддомена)

На современных платформах (Cloudflare, Netlify, Vercel) многое из этого делается автоматически. Подробнее: [web.dev — оптимизация LCP](https://web.dev/articles/optimize-lcp).

Если весь CSS инлайнится Nuxt, можно (экспериментально) убрать ссылки на внешние CSS из HTML. Для этого подойдёт хук в модуле или в конфиге Nuxt:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  hooks: {
    'build:manifest': (manifest) => {
      const css = Object.values(manifest).find(options => options.isEntry)?.css
      if (css) {
        for (let i = css.length - 1; i >= 0; i--) {
          if (css[i].startsWith('entry')) {
            css.splice(i, 1)
          }
        }
      }
    },
  },
})
```
