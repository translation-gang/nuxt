---
title: Плагины и middleware
description: 'Миграция плагинов и middleware с Nuxt 2 на Nuxt 3.'
---

## Плагины

У плагинов другой формат: передаётся один аргумент (`nuxtApp`).

::code-group

```ts [Nuxt 2]
export default (ctx, inject) => {
  inject('injected', () => 'my injected function')
}
```

```ts [Nuxt 3]
export default defineNuxtPlugin((nuxtApp) => {
  // now available on `nuxtApp.$injected`
  nuxtApp.provide('injected', () => 'my injected function')

  // You can alternatively use this format, which comes with automatic type support
  return {
    provide: {
      injected: () => 'my injected function',
    },
  }
})
```

::

:read-more{to="/docs/4.x/directory-structure/app/plugins"}

::read-more{to="/docs/4.x/api/composables/use-nuxt-app"}
Подробнее о структуре `nuxtApp`.
::

### Миграция

1. Переведите плагины на хелпер `defineNuxtPlugin`.
2. Удалите из массива `plugins` в `nuxt.config` записи для файлов из `app/plugins/`. Все файлы верхнего уровня в этом каталоге (и `index` в подкаталогах) регистрируются автоматически. Вместо `mode: 'client'` или `'server'` укажите это в имени файла, например `~/plugins/my-plugin.client.ts` загрузится только на клиенте.

## Route middleware

У route middleware другой формат.

::code-group

```js [Nuxt 2]
export default function ({ store, redirect }) {
  // If the user is not authenticated
  if (!store.state.authenticated) {
    return redirect('/login')
  }
}
```

```ts [Nuxt 3]
export default defineNuxtRouteMiddleware((to, from) => {
  const auth = useState('auth')
  if (!auth.value.authenticated) {
    return navigateTo('/login')
  }
})
```

::

Как в Nuxt 2, middleware из `~/middleware` регистрируется автоматически. Указывать его в компоненте нужно через `definePageMeta`, а не через опции компонента.

`navigateTo` — одна из вспомогательных функций маршрутизации.

:read-more{to="/docs/4.x/directory-structure/app/middleware"}

### Миграция

1. Переведите route middleware на хелпер `defineNuxtRouteMiddleware`.
1. Глобальное middleware (например из `nuxt.config`) поместите в `~/middleware` с расширением `.global`, например `~/middleware/auth.global.ts`.
