---
title: 'NUXT_E7010'
description: 'Ключ callOnce должен быть строкой.'
navigation: false
---

# E7010

`callOnce()` вызвали с ключом не-строкой. Ключ отслеживает, выполнялась ли функция уже — нужна непустая строка.

## Решение

```ts
await callOnce('setup', () => {
  // runs once
})
```

::read-more{to="/docs/api/utils/call-once"}
::
